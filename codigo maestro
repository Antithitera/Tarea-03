  
#include <iostream>
#include <cmath>
using namespace std;

/*
EQUIPO V-03
ANGEL TABUENCA
CARLOS ASENSIO
MERCEDES ANDUEZA
DESCRIPCIÓN
*/
const double pi=(4*atan(1));

void validaAngulo (double theta, char utheta, bool &b, double &a){
  if (utheta==s){
    theta=theta*pi/180
    b=false;}
  else if (utheta==r)
        b=true;
  else (utheta!=r && utheta!=s) 
    cout<<Unidades de theta erroneas<<endl;
  }
double fase (double s, double r, double &phi0){//Falta terminar el else para que cos() tenga el mismo signo que r
    if(s>=1)   
        return pi/2;
    else if (s<=-1)   
        return -pi/2;
    else {
      if(cosphi0=asin(s);
    }
}
double normaliza (double theta){
double n, thetaprima;
  n=(theta+pi)/2/pi;
  thetaprima=floor(theta-2*n*pi) 
  */ hay que redondear q numero entero con la funcion floor,supongo que se hace así
return thetaprima;
}
void analisis (double alpha, double theta0, double v0, double epsilon, double &thetaM, double &tau, int &k){
}
void rungekutta (double aplha, double &w, double &thetaT, double delta){
double sumathetaT;
doule sumaw;
double  thetaA; thetaB; thetaC; thetaD; wA; wB; wC; wD;

  thetaA=w*delta;
  wA=-alpha*seno(thetaT)*delta;
  
  thetaB=(w+wA/2)*delta;
  WB=-alpha*seno(thetaT+thetaA/2)*delta;
  
  thetac=(w+wB/2)*delta;
  wC=-alpha*seno(thetaT+thetaB/2)*delta;
  
  thetaD=(w+wC)*delta;
  wC=-alpha*seno(thetaT+thetaC)*delta;     */Tiene pinta de bucle porque he hechocopiar y pegar veces y luego cambiar algo,pero nose como
  
  
  thetaT=thetaT+thetaA/6+,thetaB/3+thetaC/3+thetaD/6;
  w=w+wA/6+wB/3+wC/3+wD/6
  
  
  
  

}
